import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Tài Khoản | Minh Hồng",
};

export default function AccountLayout({ children }: { children: React.ReactNode }) {
  return children;
}
